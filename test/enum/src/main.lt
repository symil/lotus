enum Test {
    Foo,
    Bar
}

enum Other {
    Foo,
    Bar
}

export fn main() {
    let a = Test.Foo;
    let b = Test.Bar;

    @log(a as int);
    @log(b as int);
    @log(a == b);
    @log(Test.Foo == Test.Bar);
    @log(Test.Foo != Test.Bar);
    @log(Test.Bar == Test.Bar);
}