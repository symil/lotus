export fn main() {
    @log_ptr(@alloc(4));
    @log_ptr(@alloc(4));
    @log_ptr(@alloc(4));
    @log_ptr(@alloc(-1));
    @log_ptr(@alloc(5));

    for i in 0..20 {
        @log_ptr(@alloc(8000));
    }
}