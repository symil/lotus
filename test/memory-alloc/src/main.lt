export fn main() {
    log_addr(@alloc(4));
    log_addr(@alloc(4));
    log_addr(@alloc(4));
    log_addr(@alloc(-1));
    log_addr(@alloc(5));

    for i in 0..20 {
        log_addr(@alloc(8000));
    }
}

fn log_addr<T>(value: T) {{
    (call $log_int)
}}