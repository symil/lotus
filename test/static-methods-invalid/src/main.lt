class Foo {
    value: string,

    static hello() {
        @log(this.value);
    }

    display() {
        @log(this.value);
    }
}

export fn main() {
    let a = Foo {};

    a.hello();

    Foo.display();
}