// no-prelude

export fn main() {
    for (index, value) in -2..2 {
        @log_int(index);
        @log_int(value);
    }

    for value in 2..4 {
        @log_int(value);
    }

    for value in 8..8 {
        @log_int(value);
    }
}