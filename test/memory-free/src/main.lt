export fn main() {
    let p1 = @alloc(1);
    @free(p1);
    let p2 = @alloc(1);

    @dbg(p1);
    @dbg(p2);

    for i in 0..10 {
        let p = @alloc(1);

        if i == 4 {
            @free(p);
        }
    }

    @dbg(@alloc(1));
}