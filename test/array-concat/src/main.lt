export fn main() {
    let left = [1, 2, 3];
    let right = [4, 5, 6, 7];
    let fusion = left + right;

    @log(fusion.len());

    for i in fusion {
        @log(i);
    }

    @log("---");

    let with_concat = [1, 3, 5].concat([2, 4, 6]);

    for i in with_concat {
        @log(i);
    }
}