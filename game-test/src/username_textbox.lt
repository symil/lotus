const PLACEHOLDER = "Username";
const DATA_KEY = "username";

pub class UsernameTextBox extends TextBox {
    static __default() -> Self {
        Self {
            ..TextBox::new(PLACEHOLDER, DATA_KEY)
        }
    }
}