export class EventOptions {
    pre_process_callback: fn(Object, Object) = none, // event, target
    intercepted: bool = false,
    duration: float = 0,
    ratio: float = 0,
    elapsed: float = 0,
}