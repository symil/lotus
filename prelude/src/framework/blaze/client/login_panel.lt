pub class LoginPanel {
    @OnRender {
        evt.view.layout()
            .center_to_bottom()
            .inner_margin(20)
            .child_height(0.15h)
            .child_aspect_ratio(5)
            .push(evt.local_data.ui.username_textbox)
            .push(evt.local_data.ui.login_button)
    }
}