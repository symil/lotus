pub class Counter {
    _next_value: int = 1,

    static new() -> Self {
        Counter
    }

    next() -> int {
        let result = self._next_value;
        self._next_value += 1;
        
        result
    }
}