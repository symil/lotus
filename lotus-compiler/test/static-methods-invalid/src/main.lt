class Foo {
    value: string,

    static hello() {
        log_string(this.value);
    }

    display() {
        log_string(this.value);
    }
}

export fn main() {
    let a = Foo {};

    a.hello();

    Foo::display();
}

fn log_string(str: string) {
    @log(str);
}