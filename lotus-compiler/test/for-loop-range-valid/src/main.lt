export fn main() {
    for [index, value] in -2..2 {
        @log(index);
        @log(value);
    }

    for value in 2..4 {
        @log(value);
    }

    for value in 8..8 {
        @log(value);
    }
}