class Foo {
    a: int = 2,
    b: int,
    c: int?
}

export fn main() {
    let a = 8;
    let v1 = Foo { };
    let v2 = Foo { a: 15 };
    let v3 = Foo { a };

    @dbg(v1);
    @dbg(v2);
    @dbg(v3);
}