export event OnUserConnect {
    server: Server,
    user: #USER_TYPE
}

export event OnUserDisconnect {
    server: Server,
    user: #USER_TYPE
}

export event OnUserRequest {
    server: Server,
    user: #USER_TYPE,
    request: Object
}

export event OnWorldInit {
    server: Server,
    world: #WORLD_TYPE,
}

export event OnWorldUpdate {
    server: Server,
    world: #WORLD_TYPE,
}

export event OnWorldClose {
    server: Server,
    world: #WORLD_TYPE,
}