export class MouseEvent extends WindowEvent {
    action: MouseAction,
    button: MouseButton,
    x: float,
    y: float
}

export enum MouseAction {
    Move,
    Down,
    Click,
    Up,
    Drag
}

export enum MouseButton {
    Left,
    Middle,
    Right
}