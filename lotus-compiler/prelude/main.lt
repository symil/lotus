// Temporary file to let the VS code extension that this is the root of the prelude

enum TheEnum {
    Foo,
    Bar,
    TheBla
}

class TheBar {
    test: int,

    static create() -> Self {
        Self {}
    }

    do_something() {

    }

    do_some_operation(a: int, b: int) -> int {
        a + b
    }

    @OnRender {
        let a = evt;
        let b = self;
    }
}

fn test() {
    let left = 8;
    let right = 14;
    let result = this_is_a_test(left, right);
    let bar = TheBar { test: 8 };
    bar.test = 4;
    bar.test = 6;
    bar.test = 6;
    bar.test = 6;

    bar.do_something();
    let a = bar.do_some_operation(left, right);

    let b = left as Array<int>;
}

fn this_is_a_test(foo: int, bar: int) -> int {
    let result = foo + bar;

    result
}