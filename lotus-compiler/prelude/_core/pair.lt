export class Pair<T, U> {
    type First = T;
    type Second = U;

    first: T,
    second: U,

    static new(first: T, second: U) -> This {
        This { first, second }
    }

    first() -> T {
        this.first
    }

    second() -> U {
        this.second
    }
}