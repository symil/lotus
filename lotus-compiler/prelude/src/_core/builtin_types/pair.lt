export class Pair<T, U> {
    type First = T;
    type Second = U;

    first: T,
    second: U,

    static new(first: T, second: U) -> Self {
        Self { first, second }
    }

    first() -> T {
        self.first
    }

    second() -> U {
        self.second
    }
}