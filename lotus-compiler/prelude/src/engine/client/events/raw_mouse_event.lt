pub event RawMouseEvent {
    action: MouseAction,
    button: MouseButton,
    x: float,
    y: float,
}

export enum MouseAction {
    Move,
    Down,
    Up,
}

export enum MouseButton {
    Left,
    Middle,
    Right
}