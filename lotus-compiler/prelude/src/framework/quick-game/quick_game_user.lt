export class QuickGameUser {
    name: string = none,
    messages: QuickGameMessage[] = [],
    is_searching_for_game: bool = false,
    game: bool, // TODO

    show_error(message: string) {
        self.messages.push(QuickGameMessage {
            content: message,
            kind: MessageKind::Error
        });
    }
}