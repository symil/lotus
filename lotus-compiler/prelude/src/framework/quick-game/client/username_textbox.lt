pub view UsernameTextBox {
    @OnRender {
        let textbox = TextBox {
            local_data: evt.local_data.ui.username,
            placeholder: "Username",
            on_enter: send_login_request
        };

        self.add_child(textbox);
    }
}