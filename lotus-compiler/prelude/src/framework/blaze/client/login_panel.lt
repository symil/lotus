pub view LoginPanel {
    @OnRender {
        self.background_color(Color::hot_pink())

        self.layout()
            .center_to_bottom()
            .inner_margin(20)
            .push(UsernameTextBox)
            .height(.15h)
            .aspect_ratio(5)
            .push(LoginButton)
    }
}