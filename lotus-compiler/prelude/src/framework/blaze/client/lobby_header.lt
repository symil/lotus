pub class LobbyHeader {
    @OnRender {
        evt.view.layout()
            .push(TitleLabel)
            .reset()
            .right_to_left()
            .push(UsernameLabel)
            .width(.15w)

    }
}