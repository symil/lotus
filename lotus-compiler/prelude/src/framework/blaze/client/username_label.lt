const USERNAME_TEXT_SIZE = .35h;

pub class UsernameLabel extends Label {
    @OnRender {
        check evt.user.is_logged();

        evt.view
            .text(evt.user.name)
            .text_size(USERNAME_TEXT_SIZE)
            .background_color(Color::blue())
    }
}