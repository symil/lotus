export class BaseWorld {
    // on_user_connect(user: #USER_TYPE) {

    // }

    // on_user_disconnect(user: #USER_TYPE) {

    // }

    // on_user_action(user: #USER_TYPE, action: Object) {

    // }

    // update(elapsed: float) {

    // }
}