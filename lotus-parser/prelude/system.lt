export type(void) system {
    alloc(block_size: int) -> ptr {
        return mem_alloc(block_size);
    }

    free(pointer: ptr) {
        mem_free(pointer);
    }

    log<T : Log>(value: T) {
        value.log();
    }
}